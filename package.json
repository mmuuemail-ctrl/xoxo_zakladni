{
  "name": "xoxo-web-data-updater",
  "version": "0.1.0",
  "description": "Desktop app to pick elements on a web page and auto-update values into a table.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . xoxo --platform=win32 --arch=x64 --overwrite"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "electron-store": "^8.2.0"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-packager": "^17.1.2"
  }
}
